{
  "name": "test-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "JavaScript Playwright test project",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
